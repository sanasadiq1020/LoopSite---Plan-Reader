"""Day 5 — the canonical building model, and the views generated from it.

Everything downstream reads from here: the 3D model, the four elevations
(Week 2), the take-off (Week 3) and the crew work package (Week 4). Nothing
downstream reads the plan again, and nothing redraws what this already holds
(Critical Rule 2 — one canonical model, no duplicate truth).
"""
