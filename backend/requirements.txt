# A range rather than one version. A hosted Space installs its own toolkit
# alongside this list, and that toolkit needs a newer FastAPI than a single
# pinned version allows — pinning it makes the install fail before anything
# runs. Tested on both ends of the range.
fastapi>=0.115.6,<1.0
uvicorn[standard]==0.34.0
python-multipart==0.0.20
PyMuPDF==1.25.1
Pillow==11.1.0
pydantic==2.10.4
paddlepaddle>=3.0
paddleocr>=3.0
opencv-python-headless>=4.10.0
pytest>=8.0
# Day 5: the canonical model written as IFC, the format the
# construction industry exchanges.
ifcopenshell>=0.8.0
